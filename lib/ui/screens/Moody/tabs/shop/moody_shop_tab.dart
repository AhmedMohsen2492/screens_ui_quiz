import 'package:flutter/material.dart';

class MoodyShopTab extends StatelessWidget {
  const MoodyShopTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}

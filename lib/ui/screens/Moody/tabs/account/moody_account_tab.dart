import 'package:flutter/material.dart';

class MoodyAccountTab extends StatelessWidget {
  const MoodyAccountTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}

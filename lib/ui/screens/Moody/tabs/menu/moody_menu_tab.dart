import 'package:flutter/material.dart';

class MoodyMenuTab extends StatelessWidget {
  const MoodyMenuTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple,
    );
  }
}

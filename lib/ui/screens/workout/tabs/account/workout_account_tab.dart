import 'package:flutter/material.dart';

class WorkoutAccountTab extends StatelessWidget {
  const WorkoutAccountTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}

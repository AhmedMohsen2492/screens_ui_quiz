import 'package:flutter/material.dart';

class WorkoutCharTab extends StatelessWidget {
  const WorkoutCharTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
